export enum FnsStatus {
  NOT_CHECKED = 0,
  SUCCESS = 1,
  ERROR = 2
}

export enum ReceiptStatus {
  HOLD = 0,
  IN_PROCESS = 1,
  VALID = 2,
  INVALID = 3
}

export enum DBSortOrder {
  ASC = "ASC",
  DESC = "DESC"
}
